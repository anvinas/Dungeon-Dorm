name: dungeon_and_dorms
description: A new Flutter project.

publish_to: 'none' # Remove this line if you plan to publish

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^8.1.1
  geolocator: ^14.0.2
  mobile: ^1.2.1
  shared_preferences: ^2.5.3

  # Add any additional packages here

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/pixel_bg2.png
    - assets/wood_texture2.png
    - assets/img/pixel_bg2.png
    - assets/MageScrollAnimation/
    - assets/Closed_Pixel_Scroll_2.png
    - assets/Mage_SliverOpen.png

